# Donation App — Backend

Express + MongoDB backend for a donation platform.

## Features
- JWT Authentication
- Donation creation
- Payment provider integration placeholder
- Webhook listener

## Setup
1. Copy `.env`
2. Install deps: `npm install`
3. Start server: `npm run dev`
